# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Environments & secrets
.venv/
.env

# Tooling caches
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.vscode/

# Demo study outputs (itemeval generate/grade/export artifacts)
studies/

# Local-only files (strategy, working memory, archived design contract) — never published
local/

# Claude Code: shared project config (.claude/settings.json + hooks) IS committed;
# personal per-machine settings are local-only and must never be.
.claude/settings.local.json

# OS
.DS_Store
